############################################
# AUTOGENERATED BY spycilab - DO NOT EDIT! #
############################################

workflow:
  rules:
  - if: ($CI_COMMIT_BRANCH == 'master')
  - if: ($CI_COMMIT_TAG =~ /^skip-.*$/)
    when: never
variables:
  test_variable:
    value: my_default
    description: This is a test variable.
stages:
- Testing
Unit Tests:
  stage: Testing
  script: ./pipeline.py run test
  rules:
  - if: (($test_variable == 'A') || ($test_variable == 'B'))
    when: always
Always Fails:
  stage: Testing
  script: ./pipeline.py run fail
Prefix Job:
  stage: Testing
  script: time --portability ./pipeline.py run prefix
  variables:
    SPYCILAB_RUN_PREFIX: 'true'
  rules:
  - when: never
Subprocess Job:
  stage: Testing
  script: ./pipeline.py run subprocess
  rules:
  - when: never
