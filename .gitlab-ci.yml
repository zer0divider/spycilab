############################################
# AUTOGENERATED BY spycilab - DO NOT EDIT! #
############################################

workflow:
  rules:
  - if: (($CI_PIPELINE_SOURCE == 'push') && ($CI_OPEN_MERGE_REQUESTS != null && $CI_OPEN_MERGE_REQUESTS
      != ''))
    when: never
  - when: always
stages:
- Testing
Unit Tests:
  stage: Testing
  script: ./pipeline.py run unit_tests
  artifacts:
    reports:
      junit: pyunit.xml
    when: always
